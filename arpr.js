import styled-components from react;
import material-ui from @muimaterial


const iwannagohome = () => {
    let answer = {...current}
    if(answer === undefined){
        console.log(Error)
    }else{
        console.log("정상작동")
    }
}

export default iwannagohome;